# Empirical v8.1 Static Homepage Recovery

- Replaces only index.html with a standalone, mobile-first page.
- All 20 stories are embedded directly in the HTML/JavaScript.
- No legacy footer/dashboard CSS files are loaded by the homepage.
- Footer is normal document flow.
- Mobile page cannot collapse behind a fixed footer.
- Other pages are preserved unchanged.
